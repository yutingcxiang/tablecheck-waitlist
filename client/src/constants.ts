export const RESTAURANT_CAPACITY = 10;

// Service Time per person
export const SERVICE_TIME = 3000; 

// Polling intervals in milliseconds
export const RESERVATION_POLLING_INTERVAL = 1000;
export const RESERVATION_LIST_POLLING_INTERVAL = 3000;

// Check-in grace period before automatic cancellation
export const CHECKIN_GRACE_PERIOD = 10000;